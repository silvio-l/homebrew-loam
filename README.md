# homebrew-loam

Homebrew tap for [loam.dev](https://getloam.dev).

```bash
brew
  install silvio-l/loam/loam
```

`Formula/loam.rb` is generated automatically by the release
  pipeline in silvio-l/loam.
